name: CI Pipeline
 
on:
  push:
    branches: [main]  # Запускать только при пуше в ветку main
 
jobs:
  build:
    runs-on: ubuntu-latest  # Используем GitHub-hosted runner с Ubuntu
 
    steps:
      - uses: actions/checkout@v3  # Клонирует код из репозитория
      - name: Установка зависимостей
        run: npm ci
    
      - name: Линтинг
        run: npm run lint

      - name: Тестирование
        run: npm run test

      - name: Сборка
        run: docker built -t ${{ secrets.DOCKER_USER }}/devops-nodejs-starter:${{ github.sha }} .
